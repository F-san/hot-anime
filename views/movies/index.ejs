<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <!-- 解决爱奇艺图片不显示问题 -->
    <meta name="referrer" content="never">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>动漫</title>
    <style>
        html,
        body,
        .container {
            width: 100%;
            height: 100%;
            margin: 0;
            padding: 0;
        }

        * {
            box-sizing: border-box;
        }

        .top {
            /* rem相对于根元素html，em相对于父元素 */
            padding: 1rem;
            background-color: darkcyan;
        }

        .top h2 {
            color: chocolate;
        }

        .item {
            display: flex;
            flex-direction: column;
            align-items: center;
            justify-content: center;
            width: 300px;
            height: 400px;
            padding: .2rem 0.4rem;
        }

        .item h5,
        p {
            padding: 0;
            margin: .2rem 0;
        }

        .item p {
            color: #999;
            font-size: .5rem;
            padding: 1rem;
        }

        .item img {
            width: 80%;
        }

        .content {
            display: flex;
            flex-wrap: wrap;
            align-items: center;
            justify-content: space-around;
        }
    </style>
</head>

<body>
    <div class="container">
        <div class="top">
            <h2>动漫</h2>
        </div>
        <div class="content">
            <!-- 循环导入样式及数据 -->
            <% data.forEach(item=>{ %>
            <div class="item">
                <img src="<%= item.img%>" alt="">
                <a target="_blank" href="/movies/show?id=<%= item.id %>">
                    <h5><%= item.title %></h5>
                </a>
                <!-- 多行文本省略号 -->
                <p><%= item.description.length>80?item.description.substr(0,80)+'...': item.description%></p>
            </div>
            <%})%>

        </div>
    </div>
</body>

</html>